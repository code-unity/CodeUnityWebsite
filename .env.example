# GA4 measurement id for codeunity.in. Without it every analytics call is a
# safe no-op, so local dev and preview builds send nothing.
# Set the same value in Vercel: Project Settings -> Environment Variables.
REACT_APP_GA_KEY=G-XXXXXXXXXX
